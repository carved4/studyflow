specVersion: "v1beta"
serviceId: "studyflow-react"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "studyflow-react-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
